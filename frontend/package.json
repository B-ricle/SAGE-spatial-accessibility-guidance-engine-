{
  "name": "sage-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "test": "node --test tests/*.test.js",
    "ios:sync": "node scripts/check-native.mjs && npm run build && cap sync ios",
    "ios:open": "cap open ios",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.3",
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  },
  "dependencies": {
    "@capacitor/core": "8.4.3",
    "@capacitor/ios": "8.4.3",
    "@supabase/supabase-js": "2.116.0",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "three": "0.186.0"
  }
}
